Run in the docker:

`docker build -t react-shop/frontend . && docker run -p 8080:80 react-shop/frontend:latest`

Install:

`npm install`

Run dev server:

`npm run dev`

Build project:

`npm run build`

Run tests:

`npm run test`

Run statistics

`npm run stats`
